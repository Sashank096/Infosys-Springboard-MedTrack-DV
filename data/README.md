# Data

- `raw/` contains the original relational HMIS CSV files.
- `cleaned/` contains the reproducible analytical outputs generated by the ETL script.

Keep source data and generated outputs separate. Do not manually edit files in
`data/cleaned/`; regenerate them with `scripts/build_datasets.py`.