# Pipeline scripts

Run these scripts from the repository root:

```bash
python scripts/build_datasets.py
python scripts/validate_datasets.py
```

Both scripts resolve paths from the repository location, so they work even when
invoked from a different current directory.